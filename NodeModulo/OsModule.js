const os=require("os");
console.log(os);
console.log(os.cpus())//Gives The Details Of Cores of cpu.
console.log(os.version());//Gives the virsion of ur system.
console.log(os.freemem());//Gives the amount free memory  the availabe in bits.
